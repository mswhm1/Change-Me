{
  "name": "change-me-clinics-vercel",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npx vercel@latest dev",
    "db:migrate": "dotenv -e .env.local -- node scripts/migrate.mjs",
    "db:import-sheets": "dotenv -e .env.local -- node scripts/import-sheets.mjs",
    "test": "node --test tests/*.test.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.0.2",
    "@vercel/blob": "^2.8.0"
  },
  "devDependencies": {
    "csv-parse": "^6.1.0",
    "dotenv-cli": "^10.0.0"
  }
}

